FIRESTORE RULES:
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /locandine/{doc} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}

STORAGE RULES:
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /locandine/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
